# SonarCloud (merged with settings in the Sonar UI)
# - Metrics snapshots are generated, not hand-maintained source
# - Script tests intentionally repeat structural assertions across platforms
# bump_patch_version + tests: intentional Path writes / tmp_path; covered by CI pytest (scripts/tests/test_bump_patch_version.py)
sonar.exclusions=marketing/data/**/*,scripts/bump_patch_version.py,scripts/tests/test_bump_patch_version.py
sonar.cpd.exclusions=scripts/tests/**/*
