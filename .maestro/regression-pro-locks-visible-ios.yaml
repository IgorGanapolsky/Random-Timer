appId: com.igorganapolsky.randomtimer
---
# Regression test: ALL Pro affordances must remain visible to free users on iOS
# (Never hide upgrade paths — revenue depends on discoverable Pro entry points.)
- launchApp:
    clearState: true
# Timer Range - PRO: 1H lock must be visible
- assertVisible:
    text: ".*PRO: 1H.*"
# Voice Callouts - PRO lock must be visible
- scrollUntilVisible:
    element: "Voice Callouts"
    direction: DOWN
    timeout: 10000
- assertVisible: "Preview Voice Callouts"
- assertVisible: "Unlock Voice Callouts"
# Sound Arsenal lock must be visible to free users
- scrollUntilVisible:
    element: "Unlock Sound Arsenal"
    direction: DOWN
    timeout: 10000
- assertVisible: "Unlock Sound Arsenal"
- takeScreenshot: evidence/ios-pro-locks-visible
- stopApp
