# Product static pages (repository mirror)

Canonical **GitHub Pages** deploy uses `marketing/site/` (see `.github/workflows/daily-growth-publishing.yml`).

These files are the **product landing**, AMP data, and `llms.txt` mirror kept out of the repository root. Update inbound links if you change paths; prefer linking to the live Pages URLs under `https://igorganapolsky.github.io/Random-Timer/…`.

**Download / deep-link** page: use `marketing/site/download/index.html` (deployed); the old root `download/` duplicate was removed.
