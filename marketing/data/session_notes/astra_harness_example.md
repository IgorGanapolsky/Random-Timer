# Astra harness example session note

source: play console (computer-use)
requirement: production track only
test: assembleDebug / CI green
tool_output: Path-Aware CI Gate pass

Searchable across context windows — do not rely on chat compaction alone.
