name: Dependabot Automerge

# Self-heal path: Dependabot (or agent heal PRs labeled auto-merge) land without CEO clicks.
on:
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review, labeled]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: dependabot-automerge-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  enable-automerge:
    if: |
      github.event.pull_request.draft == false &&
      github.event.pull_request.base.ref == 'develop' &&
      (
        github.event.pull_request.user.login == 'dependabot[bot]' ||
        contains(join(github.event.pull_request.labels.*.name, ','), 'auto-merge') ||
        contains(join(github.event.pull_request.labels.*.name, ','), 'dependencies')
      )
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Approve dependency PR
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
          PROJECT_PAT: ${{ secrets.PROJECT_PAT }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail
          # Bot PRs often need a non-author approval for branch protection.
          TOKEN="${PROJECT_PAT:-$GH_TOKEN}"
          GH_TOKEN="$TOKEN" gh pr review "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --approve \
            --body "Auto-approved: Dependabot/security dependency update (self-heal)." || true

      - name: Enable squash auto-merge
        env:
          PROJECT_PAT: ${{ secrets.PROJECT_PAT }}
          DEFAULT_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail

          # Success if merge/auto-merge is already underway from a sibling event.
          is_benign_race() {
            local out="$1"
            grep -qiE 'Merge already in progress|already enabled|Pull Request is not mergeable: the merge is already in progress|Auto merge is already enabled' <<<"$out"
          }

          enable_automerge() {
            local label="$1"
            local token="$2"
            local out
            if [[ -z "$token" ]]; then
              echo "Skipping ${label}: token missing"
              return 1
            fi
            echo "Attempting auto-merge with ${label}"
            set +e
            out=$(GH_TOKEN="$token" gh pr merge "$PR_NUMBER" \
              --repo "$GITHUB_REPOSITORY" \
              --squash \
              --auto \
              --delete-branch 2>&1)
            local rc=$?
            set -e
            echo "$out"
            if [[ $rc -eq 0 ]]; then
              return 0
            fi
            if is_benign_race "$out"; then
              echo "Treating as success: merge already in progress / auto-merge already enabled."
              return 0
            fi
            return 1
          }

          if enable_automerge "GITHUB_TOKEN" "$DEFAULT_TOKEN"; then
            exit 0
          fi
          if enable_automerge "PROJECT_PAT fallback" "${PROJECT_PAT:-}"; then
            exit 0
          fi
          echo "Unable to enable Dependabot auto-merge" >&2
          exit 1
