# 2026-03

## 2026-03-17 — Fix persisted Pro voice-callout toggle on mobile

- replaced the fake Pro `ON` label with a real persisted `voiceCalloutsEnabled` toggle on iOS and Android
- stored the voice-callout preference in each platform's timer config persistence path so it survives app restarts
- updated active timer/runtime gating so Pro voice callouts only fire when the user explicitly leaves the toggle enabled
- preserved the voice-callout preference when loop/config updates rebuild timer state during an active session
- normalized premium UI copy so `Sound Arsenal` is the user-facing label instead of `TACTICAL EXPANSION (PRO)`
- made the free Sound Arsenal preview expanded by default and auto-expanded it when Pro becomes active
- removed hardcoded Android/iOS UI purchase wiring that surfaced the legacy `elite` product name above the billing layer
- added regression coverage for mobile parity, config defaults, iOS config encoding, and Android repository persistence
