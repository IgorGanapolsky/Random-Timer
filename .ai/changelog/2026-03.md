# 2026-03

## 2026-03-17 — Technical debt audit: analytics truth + release parsing cleanup

- preserved last-known-good analytics outputs when PostHog data is degraded or missing, instead of publishing fake zeroes
- added `scripts/source_versions.py` as the shared source-of-truth parser for Android/iOS release metadata
- rewired release tooling and workflow steps to use the shared parser instead of brittle regex scraping
- hardened `scripts/hygiene-check.sh` to warn on `/tmp/` leaks in tracked files
- fixed local Python 3.9 compatibility for script tests that used modern annotation syntax without postponed evaluation
- corrected stale monetization claims in the public README and Android store description so docs match the shipped subscription model
- added regression coverage for version parsing and degraded analytics handling
