# Release 1.3.22

## Summary
Analytics and paywall iteration for Random Tactical Timer: clearer subscription diagnostics, optional value-framing experiment wiring, and immediate Pro access when users choose to upgrade—plus reliability fixes for Crashlytics export tooling and store-facing telemetry.

## Customer-visible changes
- Subscription funnel analytics so upgrade paths are measurable end-to-end.
- Paywall supports a value-framing experiment flag (defaults to control; no behavior change until configured in PostHog).
- Pro upgrade from Pro affordances opens the paywall immediately (no artificial “finish one session first” gate).
- Crashlytics export script improvements (BigQuery table listing, empty dataset handling).
- Executive metrics snapshot hygiene (generated file no longer tracked in git).

## Release operations
- Aligned Android `versionName` and iOS `MARKETING_VERSION` at `1.3.22`.
- Advanced Android source `versionCode` to `1774900004`; production CI may still compute the final Play upload code from Google Play state where applicable.
- Updated Play changelog `1774900004.txt` and App Store `release_notes.txt` for this version.
