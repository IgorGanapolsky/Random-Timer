# Release 1.3.56

## Summary
Paywall conversion fixes shipping to Play: billing refresh while the sheet is open (voice/repeat gates) and high-intent dismiss UX so qualified users reach purchase CTA instead of exiting via "Not now."

## Product
- **Android:** Poll Play billing up to 6s while paywall is visible; hide top "Not now" on high-intent gates (voice_gate, repeat_gate) where offer_selects had zero purchase attempts.
- **iOS:** PostHog error tracking autocapture; paywall parity with Android billing-refresh + dismiss UX (ships with TestFlight signoff).
- **Ops:** Telemetry proxy semantics fix for purchase-success artifacts (#1794).

## Release operations
- Android v1.3.56 targets Play production via `native-release.yml` on `release/v1.3.56` after CI green.
- iOS 1.3.56 blocked on CEO `testflight-signoff` approval (run 27303010981 waiting).
