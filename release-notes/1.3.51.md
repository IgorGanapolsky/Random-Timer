# Release 1.3.51

## Summary
**1.3.51** ships Android billing catalog resilience from develop (#1708): telemetry when Play Billing `PRODUCT_DETAILS` is unsupported, plus CI hardening with full `connectedDebugAndroidTest` on release builds (#1706).

## Product
- **Android billing:** Catalog probe telemetry when `PRODUCT_DETAILS` feature is unsupported; clearer diagnostics for empty paywall catalog on legacy Billing clients.
- **CI:** `android-release` gated on full connected instrumented test suite before Play upload.

## Release operations
- Ship: `release/v1.3.51` → `native-release` `platform=android` `android_track=production` after `internal-signoff/firebase` on this release SHA.
- Supersedes Play build **1778679352** (v1.3.50).
