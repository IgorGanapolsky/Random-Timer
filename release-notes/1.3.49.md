# Release 1.3.49

## Summary
**1.3.49** restores Android Pro checkout (batched billing catalog + retries), ships June 2026 Pro audio, removes Competition Warmup preset, and refreshes Play short-description copy.

## Product
- **Android:** Batched `queryProductDetails` by product type; deduped monthly→`elite_tactical` probe; stronger catalog retries; prepaid pending purchases.
- **Pro audio:** June 2026 hosted pack (`2026-06_conditioning_lane`) with auto-scaffold for scheduled monthly CI.
- **UX:** Competition Warmup preset removed from setup on Android and iOS.
- **Store:** Play short description no longer uses "coach voices" wording.

## Release operations
- Requires active Play products: `elite_tactical` (annual + P1M), `pro_base` (one-time).
- Ship: `release/v1.3.49` → `native-release` `platform=android` `android_track=production` after `internal-signoff/firebase` on release SHA.
- Post-deploy: verify PostHog `billing_product_catalog_status` → `ok` on `$app_version` 1.3.49+ (telemetry only — not ledger revenue).
