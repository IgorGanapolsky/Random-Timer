# Release 1.3.50

## Summary
**1.3.50** ships the audio regression fix from develop (#1697): restored female voice and alarm assets on Android and iOS. Includes CI guard for iOS metadata-sync when no editable ASC version exists (#1698).

## Product
- **Audio:** Restore female voice and alarm asset wiring removed in the 1.3.49 train.
- **CI:** metadata-sync fails fast when App Store Connect has no editable version (no silent skip).

## Release operations
- Ship: `release/v1.3.50` → `native-release` `platform=android` `android_track=production` after `internal-signoff/firebase` on this release SHA.
- Supersedes Play build **1778679351** (v1.3.49 tag at bd95d078 without audio fix).
