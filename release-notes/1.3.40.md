# Release 1.3.40

## Summary
UX fixes from CEO feedback: timer range parity, voice preview, volume-key alarm control, and paywall clarity. Male voice contract switches to Clyde (ElevenLabs); bundled MP3 regen pending provider billing.

## Customer-visible changes
- Android timer range minimum gap is 5 seconds (matches iOS).
- Male/female voice selection plays a preview on tap.
- Volume up/down silences the alarm during an active drill.
- Removed confusing Pro-only Training Presets section; Competition Prep remains free.
- Paywall and Android billing gating improvements.

## Release operations
- Clyde voice ID in `voice_personas.json`; regenerate bundled male MP3s via `generate-base-voice-callouts` when ElevenLabs `payment_issue` is cleared.
- Maestro paywall flow hardened for CI simulators.
