# Release 1.3.53

## Summary
**1.3.53** ships PostHog error tracking on Android and fixes executive metrics HogQL queries that returned PostHog API `http_400` (#1749).

## Product
- **Android:** PostHog `$exception` autocapture for non-internal production installs via `PostHogAnalyticsConfigFactory` and `posthog-android` 3.47.4.
- **Ops:** Executive metrics HogQL uses `toFloatOrZero` instead of invalid `toFloat64OrZero` for `paywall_revenue_sum_30d`; restores live snapshot queries.

## Release operations
- Ship: `release/v1.3.53` → `internal-signoff/firebase` on this SHA → `native-release` `platform=android` `android_track=production`.
- Supersedes Play build **1778679351** (v1.3.52).
