# Release 1.3.33

## Summary
Version 1.3.33 is a revenue-recovery release that carries the approved mobile paywall product fix from `develop` into a store-uploadable version line. It advances both Android and iOS source versions beyond the App Store Connect 1.3.32 pre-release train so the internal distribution workflow can upload a fresh build instead of failing the iOS lineage guard.

## Customer-visible changes
- Fixes the mobile paywall product mapping so approved Pro and Elite purchase options are shown correctly.
- Opens the Sound Arsenal upgrade prompt immediately instead of waiting on a stale telemetry reload path.
- Keeps the Competition Warmup release content in the unified mobile release line.

## Release operations
- Bumps Android and iOS marketing versions to 1.3.33 in lockstep.
- Keeps iOS build number 451, which is ahead of the highest App Store Connect build observed for the current highest version during the failed 2026-05-13 internal distribution run.
- Adds Android changelog metadata for source versionCode 1777931559.
